STAGED=$(git diff --cached --name-only --diff-filter=ACM)
if [ -n "$STAGED" ]; then
  echo "Scanning staged files for secrets..."
  echo "$STAGED" | xargs npx secretlint --secretlintrc .secretlintrc.json
fi
